let numeri = document.getElementById("numeri");
let arr = [1, 2, 3, 4, 50, 60, 7, 8, 90, 10];
for (let i = 0; i < arr.length; i++) {
   if(arr[i] >10) {
    numeri=arr[i];
    console.log(arr[i]);
   }
}
